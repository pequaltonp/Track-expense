# Spring Actuator 'info' endpoint values
management.endpoints.web.exposure.include=*

# Spring Actuator 'info' endpoint values
info.app.name = Track expenses API
info.app.description = Helps to track and manage your money expenses
info.app.version=1.0.0

# Spring Security
spring.security.user.name=beko
spring.security.user.password=luffy

# Datasource values
spring.datasource.url = jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=biba0218

# Hibernate
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQL10Dialect
spring.jpa.hibernate.ddl-auto = update
spring.jpa.generate-ddl=true

# Banner
spring.banner.location = my banner.txt


